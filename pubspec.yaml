name: flutter_app_pads00
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http:
  audioplayers: ^0.17.0
  like_button: ^1.0.4
  camera:
  path_provider:
  path:
  image_picker: ^0.6.7+14
  cupertino_icons: ^0.1.3

  provider:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  assets:
    - assets/
    - assets/audios/
    - assets/images/
    - assets/audios/piano/
    - assets/audios/xylophone/
    - assets/audios/drum/